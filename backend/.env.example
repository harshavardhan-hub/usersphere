# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (Use either DATABASE_URL or individual credentials)
# DATABASE_URL=postgresql://username:password@localhost:5432/usersphere

# OR use individual credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=usersphere
DB_USER=postgres
DB_PASSWORD=password

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=debug
